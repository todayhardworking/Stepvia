# Stepvia 🚀

**Turn your dreams into actionable steps.**

Stepvia is an AI-powered productivity application designed to help users break down overwhelming goals into manageable, bite-sized tasks. Powered by **Google Gemini AI**, it generates personalized action plans, helps you track progress with gamified elements, and keeps you synchronized across devices using **Firebase**.

![Stepvia Dashboard](https://via.placeholder.com/800x400?text=Stepvia+Dashboard+Screenshot)

## ✨ Key Features

- **🤖 AI Plan Generation**: Input any goal (e.g., "Learn Python", "Run a Marathon") and get a structured, step-by-step roadmap generated by Gemini AI.
- **🪄 Magic Breakdown**: One-click "Break Down" feature to decompose complex steps into micro-tasks.
- **☁️ Cloud Sync**: Seamlessly save your data to the cloud with Firebase Firestore. Access your goals on any device.
- **🔐 Secure Authentication**: Easy login via Google Sign-In.
- **📊 Progress Tracking**: Visual dashboards with charts to visualize your active vs. completed goals.
- **🎮 Gamification**: Earn XP for completing tasks and level up your productivity profile.
- **🧘 Focus Mode**: Distraction-free view for deep work on a single text.
- **📅 Smart Schedules**: Supports Daily, Weekly, Monthly, or One-time tasks with streak tracking.
- **🌗 Dark Mode**: Fully responsive UI with automatic dark mode verification.

## 🛠️ Tech Stack

- **Frontend**: [React 19](https://react.dev/), [TypeScript](https://www.typescriptlang.org/), [Vite](https://vitejs.dev/)
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)
- **Backend / Database**: [Firebase Authentication](https://firebase.google.com/products/auth), [Cloud Firestore](https://firebase.google.com/products/firestore)
- **AI Integration**: [Google Gemini API](https://ai.google.dev/)
- **Deployment**: Vercel (recommended)

## 🚀 Getting Started

Follow these steps to run Stepvia locally on your machine.

### Prerequisites

- Node.js (v18 or higher)
- npm or yarn
- A Firebase Project
- A Google Cloud Project with Gemini API enabled

### Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/yourusername/stepvia.git
   cd stepvia
   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Configure Environment Variables**
   Create a `.env` file in the root directory (copy `.env.example`):
   ```bash
   cp .env.example .env
   ```
   
   Fill in your API keys:
   ```env
   # Google Gemini AI
   VITE_API_KEY=your_gemini_api_key

   # Firebase Configuration
   VITE_FIREBASE_API_KEY=your_firebase_api_key
   VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
   VITE_FIREBASE_PROJECT_ID=your_project_id
   VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
   VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
   VITE_FIREBASE_APP_ID=your_app_id
   ```

4. **Run the Development Server**
   ```bash
   npm run dev
   ```
   Open `http://localhost:3000` to view the app.

## 🔒 Security Requirements

### Firestore Rules
To secure your user data, ensure your Firestore rules in the Firebase Console are set to:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      // Only allow users to read/write their own data
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### Authentication Headers (Local Development)
This project uses `Cross-Origin-Opener-Policy: unsafe-none` in `vite.config.ts` to allow Google Sign-In popups to work smoothly during local development.

## 🤝 Contributing

Contributions are welcome! Please feel free to submit a Pull Request.

1. Fork the project
2. Create your feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit your changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📄 License

Distributed under the MIT License. See `LICENSE` for more information.
